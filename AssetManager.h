#pragma once

class AssetManager {
public:
	//AssetManager();
	//~AssetManager();

	void loadAssets() {}
	void unloadAssets() {}

	// other asset management functions like loading specific assets, accessing assets etc.
};

#pragma once
#include "string"

class SoundManager {
public:
	//SoundManager();
	//~SoundManager();

	void playSound(const std::string& soundName) {}
	void stopSound(const std::string& soundName) {}

	// other sound related functions like controlling volume, managing music tracks etc.
};
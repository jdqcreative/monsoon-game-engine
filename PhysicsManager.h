#pragma once

class PhysicsManager {
public:
	//PhysicsManager();
	//~PhysicsManager();

	void update(float deltaTime) {}

	// other physics-related functions like adding physics bodies, applying forces, etc.
};